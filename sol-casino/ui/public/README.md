# Public Assets

Place your logo image here as `logo.png`.

The logo should be:
- PNG format (with transparency preferred)
- Reasonable size (recommended: max 800px width)
- Named exactly: `logo.png`

The UI will automatically display it in the header.
